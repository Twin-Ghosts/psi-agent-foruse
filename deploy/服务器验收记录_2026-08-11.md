# psi-cloud 服务器侧验收记录

| 项 | 值 |
|---|---|
| 执行时间 | 2026-08-11 12:03–12:22 CST |
| 服务器 | `root@auth.example.com`（<服务器公网 IP> / IPv4 A 记录已就位） |
| 主机 | `<主机名>`，阿里云 ECS，Ubuntu 26.04 LTS |
| 连接方式 | paramiko（本机无 sshpass，ssh 交互式密码无法在后台驱动） |
| 执行人 | Kiro |

## 部署实况（与文档差异）

`deploy/验收清单.md` 假定的是本仓库 `psi-agent-auth` 的 `docker compose`（含独立 `backup` 服务、`/api/auth/otp` 路由）。服务器上实际运行的是**更新形态的 `psi-cloud`**，差异如下，验收命令已按实况调整：

- 项目根：`/srv/psi-cloud/`，compose 文件 `docker-compose.yml` 两个服务 `auth`(127.0.0.1:8081) + `analytics`(127.0.0.1:8082)。
- 镜像 `psi-cloud:latest`，入口 `uvicorn psi_cloud.auth.app:app --forwarded-allow-ips *`。
- 数据 bind mount：`/srv/psi-cloud/data/auth/auth.db → /data`。
- Caddy 是**系统级** `/etc/caddy/Caddyfile`（非 compose 内），`systemctl` 托管，active+enabled。
- 认证路由：`/otp` `/verify/email` `/verify/phone` `/complete` `/logout` `/me` `/sessions` `/healthz`（非文档的 `/api/auth/*`）。
- provider 当前 `sms=mock`、`email=mock`（真实凭据未启用）。

## 结论总览

| # | 清单项 | 判定 |
|---|---|---|
| 1 | HTTPS 可访问 + 证书自动签发 | ⚠️ 部分通过（HTTP→HTTPS 跳转异常） |
| 2 | 测试钩子不可从公网访问 | ✅ PASS |
| 3 | 重启不丢库 | ✅ PASS |
| 4 | Caddy 透传真实客户端 IP | ✅ PASS |
| 5 | 备份真的可恢复 | ✅ PASS（但定时器未安装，见下） |

另有两项**部署缺口**需跟进：HTTP→HTTPS 跳转、备份定时器。

## 附：额外发现（不属五条，但影响交付）

- **业务逻辑仍是脚手架**：`POST /otp`、`POST /verify/email` 返回 `501 {"detail":"业务实现待补齐(脚手架阶段)"}`。发码/校验链路尚未实现，因此第 4 条无法经 `send_quota` 桶验证，改用访问日志中的客户端 IP 作证据（见第 4 条）。
- `/me`、`/sessions` 返回 401（鉴权在位）。

---

## 逐条明细

### 1. HTTPS 可访问 + 证书自动签发 — ⚠️ 部分通过

```
$ curl -sS -o /dev/null -w '%{http_code} ssl_verify=%{ssl_verify_result}\n' https://auth.example.com/healthz
200 ssl_verify=0                     # 证书链校验通过（未用 -k）

$ curl -sSI https://auth.example.com/healthz | grep -iE 'strict-transport|x-content-type|x-frame|referrer-policy'
Referrer-Policy: strict-origin-when-cross-origin
Strict-Transport-Security: max-age=31536000; includeSubDomains
X-Content-Type-Options: nosniff
X-Frame-Options: DENY                 # 四条安全头齐全

$ curl -sS -o /dev/null -w '%{http_code} -> %{redirect_url}\n' http://auth.example.com/healthz
403 ->                               # 期望 308 -> https://...，实际 403 且无跳转
```

- ✅ 证书自动签发有效、证书链校验通过（`ssl_verify=0`）。
- ✅ 四条安全头齐全。
- ❌ **HTTP→HTTPS 跳转失败**。`/etc/caddy/Caddyfile` 里那个 `:80 { root * /usr/share/caddy; file_server }` 默认站块接管了对 80 端口按域名未匹配的请求，导致 `http://auth.example.com/healthz` 命中静态站而非跳 HTTPS，返回 403。参考 `deploy/Caddyfile.reference` 的设计是「80 自动 301→443」。**跟进**：为 80 端口的该域名显式配置 `redir https://{host}{uri}`，或把 vhost 的 `auth.example.com` 站块同时绑定到 80/443。

### 2. 测试钩子不可从公网访问 — ✅ PASS

```
$ curl -sS -o /dev/null -w '%{http_code}\n' https://auth.example.com/__test__/provider_calls
404                                  # 期望 404，不回显验证码
```

### 3. 重启不丢库 — ✅ PASS

```
# 写入哨兵
docker exec psi-auth python -c "... INSERT OR REPLACE INTO invitation_codes(code,created_at) VALUES ('SMOKE-1', datetime('now')) ..."
→ 写入完成

# 完全重建容器（down 删容器+网络，再 up）
cd /srv/psi-cloud && docker compose down && docker compose up -d
→ psi-auth / psi-analytics Removed 后重新 Created/Started
→ psi-auth Up 12 seconds (healthy)

# 重建后校验
docker exec psi-auth python -c "... SELECT code FROM invitation_codes WHERE code='SMOKE-1' ..."
→ SMOKE-1 是否还在: True          # 期望 True
```

数据在 bind mount `/srv/psi-cloud/data/auth`，不随容器层丢失。测试哨兵已在收尾时清除。

### 4. Caddy 透传真实客户端 IP — ✅ PASS

`send_quota` 桶因业务是脚手架（`/otp` 返回 501）无法落数，改用**应用访问日志中的客户端 IP** 作证据：

```
# 从本机公网向 https://.../otp 与 /healthz 发请求后：
docker logs psi-auth | tail
INFO: <客户端公网 IP>:0 - "POST /otp HTTP/1.1" 501 Not Implemented
INFO: <客户端公网 IP>:0 - "GET /healthz HTTP/1.1" 200 OK
INFO: 127.0.0.1:xxxxx - "GET /healthz ..."   # 这些是 Caddy 本机 healthcheck
```

uvicorn 以 `--forwarded-allow-ips *` 读取 `X-Forwarded-For`，日志记录的是**真实公网 IP `<客户端公网 IP>`**，而非 `172.x` 容器网段或 Caddy 的 `127.0.0.1`。透传链路成立，限频不会共用一个桶。

### 5. 备份真的可恢复 — ✅ PASS

```
$ /srv/psi-cloud/deploy/backup.sh
backed up auth -> /srv/psi-cloud/backups/auth-20260811-122112.db.gz
backed up analytics -> /srv/psi-cloud/backups/analytics-20260811-122112.db.gz
retention: kept last 14 days

# 取最新 auth 备份，解压当库校验
gunzip -c auth-20260811-122112.db.gz > /tmp/restore-auth.db
integrity: ok                        # 期望 ok
表数: 7                              # 期望 7（users/identities/devices/sessions/email_codes/send_quota/invitation_codes）
users: 0
SMOKE-1 在备份里: True               # 备份是当前库真实快照
```

备份用 `VACUUM INTO`（非 cp，WAL 安全）。临时恢复文件已清除。

---

## 待跟进（部署缺口，非业务）

1. ~~**HTTP→HTTPS 跳转（第 1 条）**~~ → 已处理，见「修复记录」。根因是**阿里云 ICP 备案边缘拦截**，非配置问题。Caddy 侧已修好，公网放行需完成域名备案。
2. ~~**备份定时器未安装（第 5 条相关）**~~ → ✅ 已修复，见「修复记录」。
3. **业务逻辑脚手架（阻断真实链路验收）**：`/otp`、`/verify/email` 等发码/校验端点返回 501。文档验收清单第 5、6 步「真实收到邮件/短信」在业务实现补齐 + 真实凭据注入前无法验。当前 provider = mock。**（未处理，属业务开发范畴）**

---

## 修复记录（2026-08-11 12:35 前后）

### 缺口 2：备份定时器 — ✅ 已修复

单元文件此前已在 `/etc/systemd/system/`，只差启用：

```
$ systemctl daemon-reload && systemctl enable --now psi-cloud-backup.timer
$ systemctl is-enabled psi-cloud-backup.timer   → enabled
$ systemctl is-active  psi-cloud-backup.timer   → active
$ systemctl list-timers psi-cloud-backup.timer
NEXT: Wed 2026-08-12 03:17:00 CST   ACTIVATES: psi-cloud-backup.service
```

每日 03:17 自动 `VACUUM INTO` 备份已生效。

### 缺口 1：HTTP→HTTPS 跳转 — ⚠️ Caddy 已修好，公网被 ICP 备案拦截挡住

**改动**：`/etc/caddy/Caddyfile` 新增一个比 `:80` catch-all 更具体的域名跳转块（原文件已备份为 `/etc/caddy/Caddyfile.bak-20260811`）：

```caddyfile
http://auth.example.com {
	redir https://{host}{uri} permanent
}
```

`caddy validate` 通过，`systemctl reload caddy` 生效。**服务器本地验证成功**：

```
$ curl -H 'Host: auth.example.com' http://127.0.0.1:80/healthz
code=301 redir=https://auth.example.com/healthz   # 跳转块生效
```

**但公网仍返回 403**，根因经证据链坐实为**阿里云 ICP 备案边缘拦截**（不是 Caddy）：

| 请求 | 结果 | 说明 |
|---|---|---|
| 公网 `http://账域名/healthz` | 403 阿里云备案页 | `aliyun.com/beian/beian-block` |
| 公网 `http://<服务器公网 IP>/`（裸 IP 无 Host） | 200 | Caddy :80 静态兜底正常，端口本身可达 |
| 公网 `http://<服务器公网 IP>/` 带 `Host: 账域名` | 403 备案页 | 只要 Host 是该域名，边缘就在 80 明文层拦截 |
| 公网 `https://账域名/healthz` | 200 | 443 加密层不受 80 明文备案拦截影响 |
| 服务器本地 `http://账域名` | 301→https | Caddy 配置正确、已生效 |

**结论**：Caddy 配置正确且已生效；公网 80 端口的请求在到达 Caddy 之前被阿里云按「域名 ICP 备案不合规」拦截。443/HTTPS 始终正常。

**后续（根治需备案，属合规而非技术）**：
1. 完成 / 修正 `auth.example.com` 的 ICP 备案（阿里云中国大陆节点硬性要求）。备案生效后现有 Caddy 配置无需再改，80 跳转自动放行。
2. 客户端按方案是 **HTTPS 直连、不经浏览器跳转**，故 80 跳转失效**对实际认证链路无影响**；此跳转仅为手输网址的浏览器用户兜底。
3. 短期无法备案时，可考虑换香港/海外节点或已备案域名。

## 收尾状态

- 测试哨兵 `SMOKE-1` 已从 `invitation_codes` 删除，库恢复干净（users=0）。
- `/tmp/restore-auth.db` 临时文件已删。
- 服务健康：`{"ok":true,"service":"auth","smsProvider":"mock","emailProvider":"mock"}`。

## 本次对服务器的改动（可回滚）

1. `/etc/caddy/Caddyfile`：新增该域名 80 端口显式跳转块 + 清理乱码注释。原文件备份 `/etc/caddy/Caddyfile.bak-20260811`；回滚：`cp /etc/caddy/Caddyfile.bak-20260811 /etc/caddy/Caddyfile && systemctl reload caddy`。
2. 启用 `psi-cloud-backup.timer`（`systemctl enable --now`）；回滚：`systemctl disable --now psi-cloud-backup.timer`。
3. 验收过程中执行过一次 `docker compose down && up`（第 3 条要求）与手动 `backup.sh`，均属受控可逆操作，服务已恢复正常。
