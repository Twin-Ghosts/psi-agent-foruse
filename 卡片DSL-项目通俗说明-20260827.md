# 卡片 DSL 是什么 —— 一份通俗说明

写给不熟悉这套代码的人。读完你会明白：这个项目在解决什么麻烦、它长什么样、一张卡片是怎么从"几行文字"变成飞书里能点的卡的。

---

## 一、一句话概括

> 让业务只写"卡片上有什么"（10 行 XML），一个引擎自动把它翻译成飞书能识别的复杂 JSON，并且把飞书那些"反人类"的协议坑全部替你填平。

好比：你写菜单上的菜名（"番茄炒蛋"），厨房（引擎）负责买菜、切配、下锅、摆盘。你不用管灶台怎么开火。

---

## 二、它解决的痛点

飞书的交互卡片（就是聊天里那种带按钮、能打分、能填字的消息）本身是一大坨 JSON，手写起来又长又容易错。更麻烦的是飞书有几个"坑"：

1. **按钮点一次就废了** —— 飞书的回调按钮点击一次后就永久失效，卡片没法反复操作。
2. **输入框的值收不到** —— 光有输入框、不配一个"确认"按钮，用户打的字根本传不回来。
3. **引号会把卡片撑爆** —— 标题或人名里带个引号 `"`，拼进 JSON 就把结构截断了，整张卡发不出去。

以前每写一种新卡片，这些坑都要重新踩一遍。这个项目把坑一次性填平，做成可复用的"积木"。

---

## 三、三层结构（整个项目的骨架）

```
   业务写的 XML                渲染引擎                    飞书能吃的 JSON
 ┌──────────────┐        ┌──────────────────┐        ┌──────────────────┐
 │ <card>       │        │  1. 解析 XML      │        │ {                 │
 │  <info/>     │  ───▶  │  2. 按 XSD 校验    │  ───▶  │  "schema":"2.0", │
 │  <score/>    │        │  3. 编译成 JSON    │        │  "header":{...}, │
 │  <comment/>  │        │  4. 填平飞书协议坑  │        │  "body":{...}    │
 │  <button/>   │        │                   │        │ }                │
 │ </card>      │        │  产出 card+handlers│        │ + 动作映射表      │
 └──────────────┘        └──────────────────┘        └──────────────────┘
   "写什么"                  "怎么翻译"                    "飞书渲染"
```

- **第一层 XML DSL**：业务只声明"卡上有什么"，绝不出现任何飞书 JSON 概念，也不写颜色。
- **第二层 渲染引擎**：解析 → 校验 → 编译 → 填坑。当前只接飞书后端，预留了将来接 HTML 等其他后端的空间。
- **第三层 飞书卡片 JSON**：引擎的产物，直接交给飞书发卡工具。

---

## 四、积木词汇表（8 个元素）

业务能用的"积木"就这 8 种，组合出各种卡：

| 元素 | 干什么 | 例子 |
|---|---|---|
| `card` | 根容器，定标题和卡头配色 | `<card title="TODO 评价" template="blue">` |
| `info` | 一行信息 | `<info label="执行人" value="黄子建"/>` |
| `score` | 打分按钮组，能多轮改分 | `<score min="1" max="5" bind-record="recX"/>` |
| `comment` | 评语输入框（引擎自动配"确认"按钮） | `<comment placeholder="写点评语"/>` |
| `action-row` | 按钮行容器 | `<action-row>…</action-row>` |
| `button` | 一个动作按钮 | `<button text="打回重做" type="reject" action="review_reject"/>` |
| `list` | 待办列表（todo 卡） | `<list>…</list>` |
| `row` | 列表里的一行待办 | `<row title="写方案" done="true"/>` |

**关键设计：业务只写语义，不写颜色。** 比如按钮写 `type="reject"`（"这是驳回"），引擎自动映射成红色；写 `type="accept"`（"这是通过"）自动映射成蓝色。将来要换配色，只改引擎里的一张映射表，业务代码一行不动。

---

## 五、最难的部分：多轮交互怎么办（Action 六环）

飞书按钮"点一次就废"，但评价卡需要反复操作（打个分、又改分、写评语、打回重做）。引擎用"轮次"机制解决——这是整套设计里最精巧的一环，分六步：

1. **声明**：XML 里只写两个和回调相关的词 —— `action`（这个按钮干什么）和 `bind-record`（关联哪条记录）。
2. **生成带轮次的动作名**：引擎把动作名改写成 `{动作}_r{轮次}`，比如 `review_score_r0`、`review_score_r1`……（轮次从 0 起，上限 20）。
3. **预注册映射**：引擎一次性把全部 20 轮都注册到对应的处理工具上，所以哪一轮点都有人接。
4. **回调值组装**：按钮被点时，回传的数据里自动带上 `record_id`（来自 bind-record）+ 业务字段（负责人、任务等）+ 动作名 + 轮次 +（打分的还带分数）。
5. **反解**：收到回调后，从这坨数据里把字段拆解出来，知道"谁、对哪条记录、第几轮、做了什么"。
6. **状态重建**：改一下 `selected`（高亮选中的分数）和轮次号 +1，用**同一份模板**重新渲染，原位更新卡片。于是分数高亮、评语回填、按钮复活——卡片看起来"活"了。

一句话：**飞书按钮是一次性的，引擎靠"每次操作换一批全新的按钮名"来伪装成可反复点。**

---

## 六、目录结构（东西都在哪）

真正的代码在 `psi-agent-pika/examples/haitun-workspace/` 下：

```
skills/card-dsl/                    ← 给"海豚"AI 看的技能包
├── SKILL.md                        ← 词汇表 + 用法说明（AI 照着这个生成 XML）
├── card.xsd                        ← 机器可读的语法规范（带注释，可喂给 AI）
└── templates/                      ← 固定卡型的 XML 骨架
    ├── review-card.xml             ← 评价卡模板
    └── todo-card.xml               ← 待办卡模板

tools/
├── _card_dsl.py                    ← 引擎本体（解析/校验/编译/填坑），约 552 行
└── feishu_card_render.py           ← 对外入口工具（约 87 行）
```

**两种用法**：
- **固定卡型走模板**（首选）：`feishu_card_render(template="review-card", values_json={...})`，只填数据、不碰结构。
- **新卡型手写 XML**：模板覆盖不了的场景，直接写 10 行 XML 传 `card_xml=...`。

引擎入口只吃一个 XML 字符串，所以卡片定义将来从文件搬到数据库也不影响引擎——这就是"数据库预留"。

---

## 七、目前的真实状态（别被文档带偏）

截至 2026-08-27，对照仓库实际代码，有两点要如实说明（详见同目录《核对-修正报告》）：

- **评价卡** ✅ 真的全切到 DSL 了：发卡和重建共用同一个 `review-card` 模板，不存在两套会各自漂移的代码。
- **待办卡（todo 卡）** ⚠️ 模板文件在，但**生产代码还没真正调用它**，实际发卡仍走老的手写 JSON。也就是"todo 卡也由 XML 生成"这个里程碑，代码层面还没兑现。
- **实卡真人点击验证**：需要飞书租户凭证，代码层核对做不了，得在飞书里真人点一遍才算最终验收。

---

## 八、一个完整例子串起来

业务想发一张评价卡，写这 10 行：

```xml
<card title="TODO 评价" template="blue">
  <info label="执行人" value="黄子建"/>
  <info label="任务" value="优化TODO方案"/>
  <score min="1" max="5" bind-record="recXXX" selected="4"/>
  <comment placeholder="写点评语" bind-record="recXXX"/>
  <action-row>
    <button text="打回重做" type="reject" action="review_reject"/>
  </action-row>
</card>
```

引擎收到后：校验合法 → 编译成几百行飞书 JSON → 打分按钮自动生成 20 轮动作名 → 评语框自动配上"确认"按钮 → 人名/标题里的引号自动转义 → 返回 `{card, handlers}`。业务把它交给飞书发卡工具，一张能打分、能写评语、能打回、能反复操作的卡就出现在聊天里了。

业务全程没碰一行飞书 JSON，也没管任何协议坑。这就是这套 DSL 的价值。
