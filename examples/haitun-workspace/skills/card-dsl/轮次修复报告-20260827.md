# 评价卡「轮次+1」修复报告

- 日期：2026-08-27
- 对象：海豚对抗审查报告「发现 1 / 发现 2」——评价卡六环「轮次+1」未落地，改分被 Channel 墓碑静默拦截
- 结论：**指控属实，已修复。** 修复后新增测试通过，过往测试全过，已提交推送（`e673c4b`）。

---

## 零、背景与目的（这次改动到底在解决什么）

### 卡片 DSL 是什么
海豚（飞书机器人）所有人机交互都落在飞书交互式卡片上。飞书卡片有几个协议坑：**按钮点一次就永久失效、输入框值靠 confirm 才回传、引号会撑爆 JSON**。卡片 DSL 让业务只写 ~10 行 XML 声明「卡上有什么」，由渲染引擎编译成飞书那几百行 JSON 并统一填平这些坑。

### 这几轮工作的性质
不是从零写这套 DSL，而是 **独立核对「已宣称完成」的代码 → 找 bug → 修 bug → 用测试证明修好且没搞坏别的**。链路是：核对代码 → 汇总全部需求/测试点 → 补手动测试计划 → 实测 108 项 + 6 处修复 → 海豚出对抗审查报告发现真问题 → 本报告修复并闭环。

### 本次要解的核心痛点：评价卡「多轮改分」失效
飞书按钮点一次就废，所以卡片要「反复能点」靠的是**轮次机制**：每次重建卡片，按钮动作名从 `review_score_r0` 换成 `r1`/`r2`……换新名字来「复活」按钮。但评价卡这条路断了——重建时轮次没 +1，动作名恒为 `r0`，导致「打了 3 分想改成 5 分」时飞书发现 `review_score_r0` 已消费，**静默忽略、改分无反应**。这正是本次修复的目标功能。

## 一、问题（承认属实）

海豚对抗审查报告的发现 1、发现 2，我逐条去代码核实后确认**属实、非虚报**：

- **发现 1（轮次+1 未落地）**：`_render_review_card`（`_review_card_impl.py:571`）签名无 `round_` 参数，内部 `render_template("review-card", ...)` 不传轮次，恒渲染 round 0。三处重建调用点（点分 L226 / 评语 L325 / 打回 L507）全部没传轮次。而函数上方注释（L18-22、L176、L266）反复声称「every rebuild bumps a round counter」——**注释与代码不符**。
- **发现 2（改分被墓碑拦截）**：评价卡 `multi_use=True`（L703）。实测渲染发现 5 个分数按钮的 `value.action` **全部是 `review_score_r0`**（仅 `action_id` 各异 `review_score_1_r0`…`_5_r0`）。而 Channel 去重键取 `value.action`（`src/psi_agent/channel/feishu/_card_action.py:292-295`、`_action_id_of` L341-350），multi_use 墓碑文件名 = `{message_id}.{action}.consumed`（`handle_card_action` L418 传入的正是解析自 `value.action` 的 key）。
  - **推断链**：点任一分数 → 墓碑 `review_score_r0` 被消费 → 重建仍是 r0（发现 1）→ 再点任何分数（改分）命中已消费墓碑 → 被静默拦截。这正是六环「多轮改分」的核心卖点，却因轮次没加而失效。

### 更正对方两处不准确表述（不推翻结论）
1. **`_round_of` 不是全仓死代码**：`_review_card_impl.py:133` 的 `_round_of` 在评价卡内确实未被调用；但 todo 卡有**独立的** `_round_of`（`_todo_card_impl.py:93`），被 `feishu_todo_card_tick.py:86`、`feishu_todo_card_untick.py:61` 实际调用。轮次机制在 todo 卡是活的，只有评价卡这条断了。
2. **被测 HEAD**：报告抬头 `ca49c4c` 是纯 docs 提交，代码修复在其父 `283e730`。不影响结论。

---

## 二、修复（本轮改动）

### 改动文件
- `examples/haitun-workspace/tools/_review_card_impl.py`

### 改动内容
1. **`_render_review_card` 新增 `round_: int = 0` 参数**，透传给 `render_template(round_=round_)`。发卡路径（`_send_review_card`，L713 区）不传 → 保持 round 0；三处重建传 `当前轮次+1`。
2. **新增 `_round_of_value(value)` 辅助函数**：从回调 `value["round"]`（int，引擎在 `_base_value` 写入）读当前轮次，缺失时回退解析 `value["action"]`（`{action}_r{round}`）。对 `bool` 做了防御（`True` 不得被当成 round 1）。
3. **三处重建路径各加一行 `next_round = _round_of_value(value) + 1`，并传 `round_=next_round`**：
   - 点分 `_handle_score_select`（L189）
   - 评语 `_handle_review_input`（L279）
   - 打回 `_handle_review_reject`（L451）

### 为什么这样修（对齐设计意图，改动最小）
六环设计原意就是「轮次+1 → 全新 action 名 → 绕开墓碑」，注释早已写明，只是实现漏了。加 `round_` 参数是**实现设计意图的最小改动**，不动 Channel 去重逻辑（避免全局行为变更），不动引擎。

### 配套保障（工作区既有、非本轮）
`_card_dsl.py` 工作区已有一处加固 `depth = max(rounds, round_ + 1)`（`_compile` L317 区），保证「重建轮次超过模板声明 rounds」时当前轮 handler 仍注册、不成死键。此改动与本轮修复配套——见第四节高轮次证据。

---

## 三、有效性证据（本轮修复生效）

### 3.1 渲染轮次随入参递进（发卡 r0，重建递增）
```
round_=0 -> score action=review_score_r0 | handler registered=True
round_=1 -> score action=review_score_r1 | handler registered=True
round_=2 -> score action=review_score_r2 | handler registered=True
round_=3 -> score action=review_score_r3 | handler registered=True
```

### 3.2 改分场景闭环（驱动真实 handler，用假回调）
`_test_review_round.py::test_score_click_rebuilds_next_round`：
- 首次点分回调携带 round 0 → 重建卡 `value.action = review_score_r1` ✅
- 改分回调携带 round 1 → 重建卡 `value.action = review_score_r2` ✅
- 每次 action 名都是全新轮次 → **墓碑 `review_score_r{n}` 不再命中已消费项，改分放行**。

打回路径同理：`test_reject_click_rebuilds_next_round` 携带 round 2 → 重建 `review_score_r3` ✅。

### 3.3 高轮次不成死键（配合 depth 加固）
```
round_=5  -> review_score_r5  | handler registered=True (total handlers=60)
round_=7  -> review_score_r7  | handler registered=True (total handlers=60)
round_=19 -> review_score_r19 | handler registered=True (total handlers=60)
```

### 3.4 `_round_of_value` 取值正确
`test_round_of_value_reads_int_then_action`：`{"round":4}`→4、`{"round":"2"}`→2、`{"action":"review_score_r5"}`→5、`{"round":True}`→0（bool 防御）、`{}`→0，全部通过。

---

## 四、过往测试仍全过的证据（无回归，最终状态）

### 4.1 pytest 全量（5 套件 = 126 项）
```
$ PYTHONPATH=. python -m pytest _test_card_dsl.py _test_card_dsl_fixes.py \
    _test_card_dsl_spec.py _test_card_dsl_hardening.py _test_review_round.py -q -o addopts=""
126 passed
```
构成：`_test_card_dsl`(64) + `_test_card_dsl_fixes`(13) + `_test_card_dsl_spec`(31) + `_test_card_dsl_hardening`(14) + `_test_review_round`(4)。

### 4.2 直接运行（顺带修好的两处，见第七节）
```
$ PYTHONPATH=. python _test_card_dsl.py       → Ran 64 tests  OK   （原只跑 34）
$ PYTHONPATH=. python _test_card_dsl_spec.py  → 31 passed          （原跑 0）
```

### 4.3 独立验收题库（非 pytest）
```
$ PYTHONPATH=. python _psicard_testbank.py    → 18 PASS / 0 FAIL
```

### 4.4 静态检查（项目 ruff 配置：py314、line-length 120）
- `py_compile` 全部通过。
- `ruff check`（8 文件：引擎+入口+impl+5 测试）→ All checks passed!
- `ruff format --check`（同上）→ already formatted。
- 唯 `_psicard_testbank.py` 报 89 个 RUF002（中文全角标点告警，非功能问题，见第八节）。

---

## 五、仍需实卡人工验证（能力之外）

代码层已闭环，但以下仍建议在飞书里真人复核（本环境无租户凭证）：
- **改分实卡**：发评价卡 → 点 3 分 → 待重建 → 再点 5 分，确认第二次点分**生效**（分数变 5 并高亮），而非无反应。若实测仍无反应，说明线上 Channel 去重与本仓库代码不一致，请反馈实际行为。
- **评语回填 / 状态行更新 / 原位覆盖**：多轮交互下的视觉表现。
- 说明：若线上环境的 Channel 去重实际用的是 `value.action_id`（与本仓库代码不同），则原先即使不修也不会拦截——本修复对两种情况都安全（action 名带轮次后，两种去重键都天然区分各轮）。

---

## 六、本轮改动清单（已提交 `e673c4b` → `foruse/psi_card`）

| 文件 | 类型 | 改的功能 | 说明 |
|---|---|---|---|
| `_review_card_impl.py` | 改 | **评价卡多轮改分** | `_render_review_card` 加 `round_`；新增 `_round_of_value`；点分/评语/打回三处重建传 `当前轮次+1`；发卡保持 r0 |
| `_test_review_round.py` | 新增 | 轮次修复的测试 | 4 项：render 带轮次 / `_round_of_value` 取值 / 改分递进 / 打回递进 |
| `_card_dsl.py` | 改 | 引擎健壮性 | `depth=max(rounds, round_+1)` 防重建死键；`done` 布尔容错（`true/1`）对齐 XSD |
| `_test_card_dsl.py` | 改 | 测试可靠性 | `unittest.main()` 移到文件末尾（直接运行 34→64） |
| `_test_card_dsl_spec.py` | 改 | 测试可靠性 | 加 pytest 入口（直接运行 0→31）+ ruff format |
| `_test_card_dsl_hardening.py` | 新增 | 加固/对抗测试 | 14 项：XML 元字符、序列化、rounds vs round 钳制、XSD 布尔、点击可路由 |
| `_psicard_testbank.py` | 新增 | 独立验收题库 | 18 题：业务需求→XML→引擎断言三合一（非 pytest，独立跑） |
| `卡片DSL-全量需求与测试点汇总-20260827.md` | 改 | 文档 | B05 更新为「已修复+证据」，新增 B07-B10 / D06-D08 |
| `轮次修复报告-20260827.md` | 新增 | 文档 | 本报告 |

---

## 七、顺带修复的两个测试问题（海豚发现 3/4、D05）

- **发现 4 / D05（直接运行漏测试）**：`_test_card_dsl.py` 的 `unittest.main()` 原在第 308 行，其后 6 个测试类（约 30 项）在直接运行时尚未定义就退出，只跑 34/64 且不报错。已把 main 块移到文件末尾 → 现跑满 64。`_test_card_dsl_spec.py` 是 pytest 风格裸类、无 `__main__`，直接运行 0 测试，已加 `pytest.main([__file__])` 入口 → 现跑满 31。
- **发现 3（ruff format）**：`_test_card_dsl.py` / `_test_card_dsl_spec.py` 格式不通过，已 `ruff format` 修好。

---

## 八、仍需实卡人工验证（能力之外）

代码层已闭环，但以下需在飞书里真人复核（本环境无租户凭证）：
- **P0 改分实卡（最关键）**：发评价卡 → 点 3 分 → 待重建 → 再点 5 分，确认第二次**生效**（分数变 5 并高亮），而非无反应。若实测仍无反应，说明线上 Channel 去重与本仓库代码不一致，请反馈实际行为。
- **评语回填 / 状态行更新 / 原位覆盖**：多轮交互下的视觉表现。
- 说明：若线上 Channel 去重实际用 `value.action_id`（与本仓库不同），则原先即使不修也不会拦截——本修复对两种情况都安全（action 名带轮次后两种键都天然区分各轮）。

---

## 九、下一步（按优先级）

- **P0 · 实卡验证改分**（你做）：见第八节，是本次修复能否算最终通过的唯一未决门槛。
- **P1 · 让 CI 跑到 DSL 测试**：`pyproject.toml` 的 `testpaths=["tests"]` 不收集 `examples/haitun-workspace/tools/` 下的测试——测试写好且全过，但 CI 不跑，等于无人守护。需纳入 testpaths 或单开 job。
- **P2 · 补完 todo 卡 DSL 化**（黄子建）：里程碑 1 宣称「todo 卡由 XML 模板生成」，实际 `_todo_card_impl.py` 仍手写 JSON、`todo-card` 模板无生产调用。功能未落地，非 bug。
- **待定 · `_psicard_testbank.py` 的 RUF002**：89 个中文全角标点告警，已随文件进仓库。若日后纳入 CI 的 ruff 会报；可加文件级 ignore 或换半角。
- **安全 · `.env` 的真实飞书 Secret**：建议去飞书后台重置，并确认 `.env` 未进任何 git 仓库。
