# 卡片 DSL 新增功能倒排表(DDL 8.30 · 以 908 为起点重排)

- 制定:2026-08-28(周五)12:33
- DDL:**8-30 之前完成**
- 可用工期:今天下午(8-28) + 8-29(周六) + 8-30(周日)= **约 2.5 天**
- 起点:908 最新版(13 元素 + 六环 + 多轮改分墓碑绕过 + bind-field 写回 + 推卡判定,验收 23 条 + 全并集 207 全过)
- 重排原则:**时间驱动**。2.5 天内以「本机可完整验证=可确定完成」为准入线;需 lark_channel/飞书租户/CI 的项,窗口内只交付代码,验证顺延并明确标注。架构级远期(HTML 后端/触发层/定义入库)一律移出窗口。

---

## 一、时间轴倒排(从 DDL 往回排)

### 🔴 8-28 周五(今天下午,约半天)— 解除阻塞 + 起头
| 时段 | 任务 | 产出 | 验证 |
|---|---|---|---|
| 下午 | **T1 推送 908 到远程**(一切前提;远程现停 730) | 远程追平最新版 | git 一致性 |
| 下午 | **T2 模板取值域校验** `{score:1-5}` 越界报错 | 引擎+XSD+测试 | 本机全验 ✅ |
| 下午 | **T3 fuzz 扩到新元素**(divider/img/date/select/table 随机坏输入不崩) | hardening 补测 | 本机全验 ✅ |

### 🟠 8-29 周六(全天)— 纯引擎增强(本机可确定完成)
| 时段 | 任务 | 产出 | 验证 |
|---|---|---|---|
| 上午 | **T4 `<table>` 行内联操作按钮**(读表→读+对每行操作,回调带行 record_id) | 引擎+XSD+测试 | 本机全验 ✅ |
| 上午 | **T5 multi-select 多选块**(飞书多选组件) | 引擎+XSD+测试 | 本机编译验;格式发卡实测 |
| 下午 | **T6 写回可靠性:类型感知层**(日期 epoch↔字符串、单选选项名) | writeback 增强+测试 | 抽取/转换纯函数可验 ✅ |
| 下午 | **T7 bind-field 写回接进回调分发**(接线到 _card_action 路由) | 接线代码+mock测试 | 接线逻辑可验;真写顺延 |

### 🟡 8-30 周日(全天)— 收尾 + 交付固化
| 时段 | 任务 | 产出 | 验证 |
|---|---|---|---|
| 上午 | **T8 全量回归**:三版并集 + 验收脚本(23→扩到覆盖新增项)全绿 | 测试报告 | 本机全验 ✅ |
| 上午 | **T9 CI 纳入 DSL 测试**(testpaths/pythonpath 配置 + strict 装 lark_channel 说明) | CI 配置 | 配置正确性 |
| 下午 | **T10 文档固化**:SKILL 词汇表 + 完整过程报告 + 倒排表更新到最新 | 文档集 | 人工 review |
| 下午 | **T11 交付打包 + 最终推送** | 交付物 | 远程一致性 |

### ⚪ 窗口外(8-30 后 / 需外部环境,不计入本次 DDL)
- **实卡真人多轮点击终验**(需飞书租户,人工)—— 引擎侧已验,只差物理点击
- **推卡侦测层**(轮询/webhook 触发,需完整运行时)
- **HTML 渲染后端 / 定义入库 / 第三层触发层**(方案明确预留但声明本版不做)
- **radio 真互斥视觉**(增强方向,非当期)

---

## 二、准入线与砍项理由(诚实)

**为什么这么排**:2.5 天里唯一能"确定完成并给出证据"的,是本机可完整验证的纯引擎/纯函数项(T2/T3/T4/T6/T8)。所以把这些塞满窗口。

**明确砍出窗口的**(2.5 天内本机给不出完成证据):
- 真实 Bitable 写入、推卡侦测、实卡点击 —— 全依赖 lark_channel/飞书租户,窗口内只能交付代码+mock,真实验证必然顺延。硬塞进 DDL 只会是"代码写了但没验",不诚实。
- 架构级(HTML 后端等)—— 方案自己写明"本实验不做",非欠账。

**风险缓冲**:T5(multi-select)依赖飞书组件格式实测,若飞书 2.0 不支持(像之前 note 被淘汰),就地降级为"已探测淘汰"并从窗口移除,不占后续工期。

---

## 三、每日可交付里程碑(便于对 DDL 卡点)

- **8-28 收工**:908 已推送 + 取值域校验 + 新元素 fuzz,全绿 → 远程已是可交付基线
- **8-29 收工**:table 行操作 + multi-select + 写回类型感知 + 写回接线,本机全验通过
- **8-30 收工**:全量回归绿 + CI 配好 + 文档固化 + 最终交付 → **DDL 达成**

> 每天以"本机可验项全绿"为硬卡点;任一天滑坡,优先保 T1/T2/T3/T4/T8 这条主干(纯引擎可验闭环),其余(T5/T7/T9)可顺延窗口外。
