# 卡片完整功能报告（核实版）

- 报告日期：2026-08-29
- 核实范围：`examples/haitun-workspace` 下卡片 DSL 引擎、todo 卡、mentor/boss 报表卡生成器、写回与推卡模块
- 核实方式：逐项对照代码（`tools/_card_dsl.py` 等 12 个实现文件）+ 全量测试实跑 + 生成器实跑 + 对照《新增功能倒排表-8.30》
- 测试结果：**205 passed, 2 skipped**（2 项 skip 为 `xmlschema` 未安装导致的 XSD 校验用例跳过，非功能缺口）
- 分支：`work-psi_card`，工作树 clean；**本地领先 `origin/psi_card` 9 个提交（未推送）**

---

## 一、功能总览

| 功能面 | 范围 | 状态 |
|---|---|---|
| ① 卡片 DSL 引擎 | 13 元素 + Action 六环 + 轮次墓碑绕过 + 终态只读 | ✅ 完全 |
| ② 固定卡型模板 | review / todo / mentor-report / boss-overview 4 套 | ✅ 完全 |
| ③ todo 卡（list/row） | 多行逐条勾选、20 轮、undo、任务联动 | ✅ 完全 |
| ④ mentor/boss 报表卡生成器 | 7 栏目 + 台账口径 + 请假/考勤/入职真实数据通道 | ✅ 完全 |
| ⑤ bind-field 通用写回 | 抽取纯函数 + 字段/记录存在性预检 | ✅ 核心完全，类型感知层部分 |
| ⑥ 数据驱动推卡 | `decide_push` 纯判定 | ✅ 判定完全，侦测层未做（窗口外） |

---

## 二、逐项功能核实（代码级证据）

### ① 卡片 DSL 引擎 — ✅ 完全

| 功能点 | 状态 | 证据 |
|---|---|---|
| 词汇表 13 元素 `card/info/score/comment/action-row/button/list/row/divider/img/date/select/table` | ✅ | `tools/_card_dsl.py` `_compile()` 逐 tag 编译，未知元素显式报错 |
| 语义色映射（template 4 色 + button 语义色） | ✅ | `_TEMPLATE_COLORS` 全映射；`accept→primary / reject|danger→danger / default→default` |
| Action 六环（声明/轮次名/预注册/回调组装/状态重建/业务边界） | ✅ | `_compile()` 对 score/comment/button/date/select 均按 `{action}_r{round}` 预注册 `_MAX_ROUNDS` 轮 |
| 多轮改分墓碑绕过（轮次 +1 重建） | ✅ | `round_` 参数钳制 [0, `_MAX_ROUNDS-1]`，重建时 +1 |
| 轮次用尽终态只读卡 | ✅ | `_compile_terminal()`：交互元素退化为 info 行，handlers 清空（对齐 todo 卡 locked 态） |
| comment 自动 confirm + 文案可覆盖 | ✅ | `confirm-title/confirm-text` 可覆盖，缺省评语措辞 |
| `<table>` 原生表格 + `max_rows`/`more` 截断 + `empty` 空表 + 非对象行报错 | ✅ | `_table_blocks()`；`max_rows` 默认 10，`{n}` 替换总行数 |
| `divider`/`img`/`date`/`select` 元素 | ✅ | 编译为飞书 2.0 `hr`/`img`/`date_picker`/`select_static`；img-key 缺失、date/select 缺 action 均报错 |
| 多 `<list>` 声明、list 与其它元素混用 → 显式报错 | ✅ | `render_card()` 数量/混用校验 |
| 残留占位符防护（不发脏卡） | ✅ | `render_template()` 填充后扫描 `{key}`，未填全直接返回错误 |
| score `min>max` 报错 | ✅ | `_score_columns()`：`<score> min={lo} > max={hi}` 显式 raise |
| 双 score 同 action 去撞车（occurrence 折进 action_id） | ✅ | `action_occurrence` 计数 |
| XXE / billion-laughs / 非法实体防护 | ✅ | `_validate()` + `TestUntrustedXml` 4 用例 |

### ② 固定卡型模板 — ✅ 完全

`skills/card-dsl/templates/` 下 4 套：`review-card.xml`、`todo-card.xml`、`mentor-report-card.xml`、`boss-overview-card.xml`。
调用路径 `feishu_card_render(template=...)` → `render_template()` → 填占位符 → 编译。文档（SKILL.md）与 XSD（`card.xsd`，10.8KB 带注释）齐备。

### ③ todo 卡（list/row）— ✅ 完全

| 功能点 | 状态 | 证据 |
|---|---|---|
| 多行逐条勾选（每行独立 action、独立墓碑） | ✅ | `_todo_card_impl.py` `_tick_action_id`/`_untick_action_id` 逐行轮次 |
| 20 轮 tick/untick（`_UNDO_ROUNDS`） | ✅ | `_todo_card_impl.py:76` |
| 勾选/取消勾选（undo） | ✅ | `feishu_todo_card_tick` / `feishu_todo_card_untick` |
| 行内 `task-guid` 联动飞书任务完成 | ✅ | `feishu_todo_card_tick` 写 `completed_at`（毫秒 + `update_fields`） |
| 锁卡终态（轮次用尽只读） | ✅ | `_build_card_from_state()` locked 分支 |
| 模板为单一真源（发卡与重建不漂移） | ✅ | `feishu_todo_card.py` 走 `todo-card.xml` 模板 + `_build_todo_card` |

### ④ mentor/boss 报表卡生成器 — ✅ 完全

| 功能点 | 状态 | 证据 |
|---|---|---|
| 7 栏目（人员概况/目标/完成/逾期/请假/评价/趋势）+ 数据源/报表链接 | ✅ | `mentor-cards/build_cards.py`，契约 `mentor-report-card-structure.md` v2.2 |
| 统计口径纯函数 `build_mentor_stats` | ✅ | `_test_report_stats.py` 全绿 |
| 真实数据通道：请假（近 90 天审批）/ 考勤（周期窗口打卡）/ 入职时间 | ✅ | `fetch_leave_attendance.py` → `data/leave.json`/`attendance.json`/`join.json` |
| 台账口径（每 mentor 一个 base，`ledger_sources.json` + `fetch_ledgers.py`） | ✅ | 未注册组自动回落 `manual_calibration.json`（卡面标注核对时间） |
| 未按时判定链 `✅filled→🏖leave→📅not_joined→⚠️unfilled` | ✅ | `member_status()`，请假豁免自动过滤（不发不标注） |
| mentor 发卡工具 / boss 发卡工具 | ✅ | `feishu_mentor_report_send` / `feishu_boss_overview_send`（`_test_mentor_report_send.py`/`_test_boss_overview_send.py` 全绿） |
| 生成器实跑 | ✅ | 8-29 实跑产出 9 张卡（8 mentor + 1 boss），7 栏目齐全、台账总览/逾期明细在卡 |

### ⑤ bind-field 通用写回 — ✅ 核心完全 / ⚠️ 类型感知层部分

| 功能点 | 状态 | 证据 |
|---|---|---|
| 抽取纯函数 `extract_writeback`（score/date/select 选中值） | ✅ | `tools/_card_writeback.py`；date 取 `option.split()[0]`，select 取 `action.option` |
| 字段存在性预检（字段名拼错显式报错，不静默） | ✅ | 写前 `list_bitable_fields_impl` 核对 |
| 记录缺失明确区分（RecordIdNotFound/91403 单独 stage） | ✅ | `write_back_from_callback()` |
| **类型感知层（日期 epoch↔字符串、单选选项名归一）** | ⚠️ 部分 | 日期取"2026-09-02"字符串已处理；**epoch 毫秒转换、单选选项名→选项 id 未实现**（倒排表 T6 未完成） |
| **接进 `_card_action` 回调分发** | ❌ 未接线 | `write_back_from_callback` 独立可用，但未挂到 Channel 回调路由（倒排表 T7 未完成） |

### ⑥ 数据驱动推卡 — ✅ 判定完全 / ❌ 侦测层未做

| 功能点 | 状态 | 证据 |
|---|---|---|
| `decide_push` 纯判定（equals/not_equals/in/present，缺模板/收件人跳过不乱推） | ✅ | `tools/_card_autopush.py`，`_test_card_autopush.py` 全绿 |
| 记录变化 → 触发（轮询/webhook 侦测层） | ❌ | 未实现，倒排表明确标注「需完整运行时，窗口外」 |

---

## 三、8.30 倒排表进度核实（今日 8-29 实况）

| 任务 | 内容 | 状态 |
|---|---|---|
| T1 推送 908 到远程 | 远程追平最新版 | ❌ **本地领先远程 9 个提交**（3e1d0c2 等：mentor/boss 发卡工具 + 生成器运行代码 + 契约），未推送 |
| T2 模板取值域校验 `{score:1-5}` 越界报错 | 取值域校验 | ⚠️ 部分：min>max 显式报错（0ec423f）已做；**越界 1..9 仍钳制不报错**，`{score:N}` 域校验未做 |
| T3 fuzz 扩到新元素 | 坏输入不崩 | ⚠️ 部分：divider/img/date/select/table 有结构化用例覆盖（`TestDivider`/`TestNewElements`/`TestTableElement`），无随机坏输入 fuzz |
| T4 `<table>` 行内联操作按钮 | 读表升级成读+对每行操作 | ❌ 未实现（table 目前只读展示） |
| T5 multi-select 多选块 | 飞书多选组件 | ❌ 未实现（无 checker/multiSelect 元素） |
| T6 写回类型感知层 | 日期 epoch↔字符串、单选选项名 | ⚠️ 部分：date 字符串化已做，epoch/选项名转换未做 |
| T7 写回接进回调分发 | 接线到 `_card_action` 路由 | ❌ 未接线 |
| T8 全量回归（23→扩） | — | ✅ 205 passed, 2 skipped（skip 为 xmlschema 环境依赖） |
| T9 CI 纳入 DSL 测试 | — | ⚠️ 未确认（本机 pytest 已可跑全部套件，CI 配置未见） |
| T10/T11 文档固化 + 交付打包推送 | — | ⚠️ 文档已多份（本次新增本报告），打包/最终推送未做（T1 未推） |

---

## 四、测试证据

```
DSL/写回/autopush/报表 9 套件：161 passed, 2 skipped（xmlschema 未装，XSD 校验跳过）
mentor/boss 发卡 + 统计口径 3 套件：44 passed
─────────────────────────────────────────────
合计：205 passed, 2 skipped
```

覆盖套件：`_test_card_dsl` / `_test_card_dsl_fixes` / `_test_card_dsl_hardening` / `_test_card_dsl_spec` / `_test_card_dsl_strict` / `_test_card_writeback` / `_test_report_cards` / `_test_card_autopush` / `_test_review_round` / `_test_report_stats` / `_test_mentor_report_send` / `_test_boss_overview_send`

---

## 五、未实现 / 缺口清单（诚实标注）

| 缺口 | 性质 | 备注 |
|---|---|---|
| `<table>` 行内操作按钮（T4） | 倒排表 8-29 任务，**未做** | 当前 table 只读展示 |
| multi-select 多选块（T5） | 倒排表 8-29 任务，**未做** | 依赖飞书组件格式实测，有降级预案 |
| 写回类型感知层（T6 后半） | 倒排表 8-29 任务，**部分** | epoch/单选选项名未实现 |
| 写回接回调分发（T7） | 倒排表 8-29 任务，**未接线** | 独立函数可用，未挂路由 |
| 模板取值域校验（T2 后半） | 倒排表 8-28 任务，**部分** | min>max 已报错，`{score:N}` 越界仍钳制 |
| 推卡侦测层（轮询/webhook） | 声明窗口外 | 需完整运行时 |
| HTML 渲染后端 / 定义入库 / 第三层触发层 | 声明窗口外 | 方案预留 |
| 实卡真人多轮点击终验 | 需飞书租户人工 | 引擎侧已验，物理点击未做 |
| XSD 校验测试 2 项 | 环境依赖 | `xmlschema` 未安装，装上即跑 |
| 远程追平 | 未推送 | 本地领先 origin/psi_card 9 commits |

---

## 六、结论

**核心功能完全**：卡片 DSL 13 元素引擎、4 套固定模板、todo 卡逐条勾选、mentor/boss 报表卡生成器（7 栏目 + 真实数据通道 + 台账口径）均已实现且测试全绿（205 passed），生成器实跑产出 9 张卡。

**未完全项集中在 8.30 倒排表后半程**：table 行内按钮（T4）、multi-select（T5）、写回类型感知（T6 后半）、写回接线（T7）四个新增功能尚未落地；推送（T1）未执行，本地领先远程 9 commits。这些是明确的在建缺口，非未知盲区。
