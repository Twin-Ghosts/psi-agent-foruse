# 卡片 DSL 核对 —— 修正报告

- 日期：2026-08-27
- 对象：黄子建卡片 DSL 化实验
- 分支：`feat/todo-card-dsl`（force-push 后 HEAD `62a4c68`）
- 环境：Python 3.14.6（与项目 `requires-python>=3.14`、CI、ruff target py314 一致）

---

## 一、需要修正的上次结论

**L110 语法不是阻断级 BUG。**

我上次报告称 `_card_dsl.py:110` 的 `except TypeError, ValueError:`（无括号）是"Python 2 语法、无法导入、阻断级"。该结论在项目实际环境**不成立**：

- 该写法是 **PEP 758**，Python 3.14 已恢复支持。本项目 `requires-python>=3.14`、CI 三个 workflow 全用 3.14、ruff target 也是 py314。
- 实测：HEAD 原样代码在 Python 3.14.6 上 `ast.parse` 通过，**原样可导入、不需修复**。
- 我上次报 SyntaxError，是因为用系统 `python3`（3.13.5）测的。它只在 ≤3.13 挂，而项目不支持 ≤3.13，故影响面 ≈ 0。
- 补充：无括号 except 是**全 workspace 32 个文件的既有风格**，改带括号反而破坏一致性。

---

## 二、关键澄清：存在两个版本

核对中出现的"矛盾结果"，根源是混淆了两份代码：

| | HEAD（62a4c68，force-push 后干净版） | 工作区（未提交） |
|---|---|---|
| `_card_dsl.py` | 552 行，L110 无括号 except，含 2 处 pre-existing 小问题 | 580 行，含我上次的 5 处修复 |
| 测试文件 | 无 | `_test_card_dsl.py`(64) + `_test_card_dsl_spec.py`(31)，均 untracked |
| `git status` | 干净 | `M _card_dsl.py` + 两个 `??` |

我上次的修复**从未 commit**，只在工作区。所以"文件都在但 bug 还在"（指 HEAD）与"L110 已处理"（指工作区讨论）两种说法各自对应不同版本。

---

## 三、文档宣称 vs 代码事实（当前 HEAD 实测）

| 文档宣称 | 核实结果 |
|---|---|
| 4 文件 + 2 模板 | ✅ 全部就位；`_card_dsl.py` 552 行（"约 550"一致） |
| XSD 8 元素 | ✅ `card/info/score/comment/action-row/button/list/row`，属性与文档一致 |
| 语义色 accept→蓝 / reject→红、20 轮预注册 | ✅ SKILL.md 与引擎实测一致（60 handlers/卡） |
| 引擎返回 `{ok, card, handlers}` | ✅ |
| 数据库预留（`render_card` 吃 XML 字符串） | ✅ 入口即 `card_xml` 参数，来源与引擎解耦 |
| 评价卡发卡+重建全切 DSL、旧手写删除 | ✅ `_review_card_impl.py` 发卡与重建共用 `render_template("review-card")` |
| 里程碑 1：todo 卡也由 XML 模板生成 | ⚠️ **不符**：`_todo_card_impl.py` 仍是手写 JSON，`todo-card` 模板无任何生产代码调用 |
| 三卡型实卡验证（含方案评审卡） | ⚠️ 方案评审卡无模板落地仓库，无法从代码验证（架构上"只换 XML"成立） |

---

## 四、两个小问题的状态与处置决定

**① `score min>max` 不校验 —— 未修，经确认为设计选择，保持现状。**

- XSD 只约束正整数，管不了大小关系（实测 `min=5 max=1` 判 valid）。
- 引擎层静默钳制：`hi<lo` 时 `hi=lo`，只生成 1 个按钮（`_card_dsl.py:175-176`）。
- 处置：维持静默钳制，不动代码。

**② DSL 引擎零自动化测试覆盖 —— 已在本地写了覆盖套件，但按决定不进仓库/CI。**

- 工作区含 95 项测试（编译 / 轮次 / 模板 / 错误路径 / XSD 一致性 / Action 六环），本轮实测全过。
- 但文件 untracked，且需 `-o addopts=""` 绕开仓库 coverage 配置，**当前 CI 跑不到**。
- 处置：只留本地，先不动仓库。
- **因此"文档宣称的实卡验证无法在 CI 复现"这一条仍然成立**，向上反馈时应如实说明。

---

## 五、全部修复清单（工作区版，共 5 项真 bug + 1 项结论修正）

> 以下修复均在**工作区**，未 commit。HEAD（62a4c68）仍是修复前状态。
> `_card_dsl.py` 相对 HEAD：`+59 / -31` 行。

### 结论修正（非代码改动）

**0. L110 `except` 语法 —— 撤销上次的误判，代码不改。**
- 上次报"无括号 `except TypeError, ValueError:` 是阻断级 SyntaxError"，实为误判（用 3.13 测的）。
- 3.14 起 PEP 758 恢复该写法；且它是全 workspace 32 文件的既有风格。
- 结论：**保持无括号原样**。我一度改成带括号又已回滚。详见第一节。

### 5 项真 bug 修复（`_card_dsl.py`）

**① 占位符二次展开** —— 最隐蔽的字段串味 bug。
- 现象：旧的链式 `str.replace` 会把"已填入的值"再次当模板扫描，若某字段值里恰好含 `{another_key}`，会被再展开一次，一个字段的内容漏进另一个字段。
- 修法：改成单遍 `re.sub`（新增 `import re` + `_PLACEHOLDER_RE`），每个占位符只对原始 values 解析一次，替换后的文本不再被扫描；未知占位符保留字面量以便暴露模板 bug。

**② comment input `name` 撞名** —— 多评语框互相覆盖。
- 现象：飞书按 input 的 `name` 收值，同一张卡里多个 `<comment>` 若共用同一 `bind-record`，`name` 会撞（都叫 `comment_{record_id}`），值互相覆盖。
- 修法：`_comment_input` 加 `index` 参数，`_compile` 改 `enumerate(root)` 传序号，`name` 变成 `comment_{record_id}_{index}` 保证唯一。核实 `_review_card_impl` 按 `tag=="input"` 取值、不依赖 name，故无回归。

**③ 属性引号不转义** —— 最实际的"发不出卡"。
- 现象：`_xml_escape` 用 `saxutils.escape` 默认只转 `< > &`，不转引号。但所有占位符都塞在双引号属性里（`title="{...}"`），标题/人名/评语含 `"` 会截断属性、破坏整段 XML。
- 修法：`escape(text, {'"': "&quot;", "'": "&apos;"})`，显式转义单双引号。验证含引号的值填充后经 XML 解析能还原成干净引号进最终 JSON。

**④ 报错消息误导** —— 按钮缺 action 时报错指错地方。
- 现象：按钮缺 `action` 时，先撞 `_resolve_handler` 报"no handler"，盖住了真正的问题。
- 修法：在 action-row 分支里先查 `action` 非空（`raise "<button> requires an action attribute"`）再 resolve；未知元素消息补上 `list`。

**⑤ XSD 与引擎漂移** —— 空 action-row 行为不一致。
- 现象：空 `<action-row/>` 按 XSD（button minOccurs=1）非法，但引擎静默吞掉不报错。
- 修法：空 columns 时 `raise "<action-row> requires at least one <button>"`，与 XSD 对齐、不静默。

### 未修（经确认的设计选择，见第四节）

- `score min>max`：引擎静默钳制成 `hi=lo`，保持现状。

---

## 七、本轮实测汇总（工作区版）

- 引擎导入 OK；模板渲染 review-card `ok=True`；round=3 动作名全部 `_r3`。
- 错误路径正确 fail-closed：空 action-row / 按钮缺 action / 未知元素 均返回 `ok=False`。
- XSD 加载 OK，8 元素。
- 测试 95 项（两种文件加载顺序）全过；ruff（py314 全 select）三文件 All checks passed。

---

## 九、持续检查：优化点排查结论（均维持现状，不改代码）

第二轮通读引擎 + 边界实测，挖出若干可优化点，逐条调查后**全部决定维持现状**，理由如下（重点是"慎重、无副作用"）：

**A1. `rounds` 属性引擎不读（硬编码 20）** —— 保留现状。
- 若让 `rounds` per-element 生效，会破坏"全卡统一轮次"不变式：score 注册 5 轮、comment/button 仍 20 轮，重建 `round+1` 时 score 先越界丢 handler。**有副作用**。
- 现状：属性哑值恰好 =20，行为正确；作为"支持多轮改分"的自文档保留。

**A2. 同名 action 的两组 score `action_id` 撞车** —— 非功能 bug，不改。
- 实测两组 `record_id` 正确区分（recA/recB），落账落对账。
- `_review_card_impl` 落账/重建只读 `record_id`+`score`，**不读 `action_id`**（`_review_card_impl.py:197,207`）。
- 飞书墓碑认回调 value 的 `action` 字段；评价卡"点分即整卡重建、轮次+1"，点一次整轮全换名，两组撞不撞都被整体换掉。
- 结论：`action_id` 是下游无人读的冗余字段，撞车不触发任何错误。

**A3. `<info>` 的 value 未做 markdown 转义** —— 全 workspace 既有约定，不单点改。
- 成熟的 todo 卡（`_todo_card_impl.py:144`）同样把 title 直插 markdown 不转义——整个 workspace 的约定就是"正文 markdown 信任业务文本"。
- DSL 引擎单独给 info 加转义会与 todo 卡行为不一致，且飞书 markdown 转义语义无官方保证，贸然转义可能显示多余反斜杠。
- 与上次"except 语法"同类教训：先确认是不是全仓约定，别单点破坏一致性。若要修应全局统一，超出本次范围。

**B/C 类（`done` 大小写、except 风格不齐、selected 复用 `_parse_round`、`column_set` 字面量重复、预注册三段式重复）** —— 均为低价值锦上添花，按"无必要不改"原则维持现状。

> 一句话：这轮"持续检查"没有发现需要立即修的新 bug；所有候选优化经评估要么有副作用、要么破坏全仓一致性、要么价值过低，故全部维持现状。上一轮的 5 处真 bug 修复仍在且全绿。

---

## 十、海豚对抗审查报告 + zip 修正版落地（2026-08-27，最新）

海豚出具了一份独立对抗审查报告（不信任作者结论、全部复跑 + 自写 13 组对抗测试），复现 6 个问题。我逐条去代码核实，**全部属实、无一虚报**，其中 3 项戳中我此前的疏漏。随后收到用户提供的 `psi-card-修正版-20260827.zip`（含完整 psi-card-test 项目），其中 `_card_dsl.py` 已升至 **638 行** 并新增 `_test_card_dsl_fixes.py`（174 行），针对报告做了修复。已用 zip 版替换工作区 7 个文件并验证。

### 海豚报告确认属实的问题

| 编号 | 问题 | 定性 | 是否戳中我疏漏 |
|---|---|---|---|
| 测试结构 | `unittest.main()` 在第 308 行，其后 6 个类（约 30 测试）直接运行时静默不跑 | —— | ✅ 是（"95 全过"真，但换跑法只跑一半还不报错） |
| A | 多个 `<list>` 静默丢数据（只取第一个） | 中危 | ✅ 是 |
| D | 模板漏传值 → 残留占位符 `{record_id}` 当真 id 写台账 | 中危 | ✅ 是（正是我"未知占位符保留字面量"设计的副作用） |
| B/F | 同 action 双按钮/双 comment 的 `action_id` 撞车 | 低危 | 与我上轮 A2 结论一致 |
| C | `bind_record` vs `ledger_record_id` 键名两套并存 | 低危 | —— |
| E | `rounds` 属性被忽略（硬编码 20） | 低危 | 与第九节 A1 同一件事 |

### zip 修正版做的四项修复（已验证生效）

- **A 多 list** —— `render_card` 加 `list_count != 1` 报错，不再吞行。实测 `False` ✓
- **D 残留占位符** —— `render_template` 填充后剔除 `<!-- -->` 注释再扫 `{key}`，有残留即报错，不发脏卡。实测报 `有未填充的占位符: {record_id}` ✓
- **B action_id 撞车** —— `_button_element` 加 `occurrence` 参数区分同 action 多按钮。fixes 测试覆盖 ✓
- **E rounds 漂移** —— 新增 `_parse_rounds`，引擎真读 `rounds`，非法值回退 20、上限钳到 `_MAX_ROUNDS`（多注册无害、少注册会死按钮的取舍）。实测 `rounds="5"` 真的只注册 5 个 handler ✓

> 注：E 的实现回避了第九节 A1 担心的"全卡统一轮次被破坏"——它只调 score 的预注册深度且严格钳制在引擎上限内，多注册无害、少注册才危险，故安全。

### 替换与验证

- 替换 7 文件：`_card_dsl.py`(638 行)、`feishu_card_render.py`、`_test_card_dsl_fixes.py`(新增)、`card.xsd`、`SKILL.md`、`review-card.xml`、`todo-card.xml`。`_test_card_dsl.py` 与 `_test_card_dsl_spec.py` 与 zip 相同，未动。
- **验证全绿**：108 项测试（原 95 + 新 13）两种加载顺序全过；ruff（py314 全 select）All checks passed；四项修复实测逐一生效。
- `except` 语法仍保持无括号（zip 版一致，3.14 合法）。

---

## 八、仍在能力之外

飞书实卡真人多轮点击验证需租户凭证，交由你在飞书内完成——这是代码层核对无法覆盖的唯一一环。
